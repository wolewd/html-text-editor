{
  "name": "wysiwyg",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "devDependencies": {
    "@tailwindcss/cli": "latest",
    "@types/bun": "latest",
    "tailwindcss": "^4.3.0"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "wolfe": "git+ssh://git@github.com/wolewd/wolfe.git"
  },
  "scripts": {
    "dev": "bun run index.ts --dev",
    "build": "bun run index.ts --build",
    "plugin": "bun run index.ts --plugin"
  }
}
